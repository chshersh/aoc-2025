{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build (default)",
            "type": "shell",
            "command": "cmake --build --preset default",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$gcc"
        }
    ]
}
